#include <bits/stdc++.h>

using namespace std;

int main(){

    // string str = to_string(5) + "," + to_string(4);

    // cout << str << endl;

    string str = "karthik";

    cout << str.substr(1,3) << endl;
    if(str.substr(1,3)=="art"){
        cout << "true" << endl;
    }
    
}

